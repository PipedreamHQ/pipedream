const BASE_URL = "https://api.parseur.com";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_LIMIT = 50;
const DEFAULT_MAX = 600;
const WEBHOOK_ID = "webhookId";
const API_KEY = "apiKey";

const PROPERTY_TYPE = {
  STRING: "string",
  ARRAY: "array",
  OBJECT: "object",
};

export default {
  BASE_URL,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
  LAST_CREATED_AT,
  WEBHOOK_ID,
  API_KEY,
  PROPERTY_TYPE,
};
