const BASE_URL = "https://api.range.co";
const VERSION_PATH = "/v1";
const LAST_PUBLISHED_AT = "lastPublishedAt";
const DEFAULT_LIMIT = 1000;

const PAGINATION = {
  STATE: {
    END: "END",
  },
};

export default {
  BASE_URL,
  VERSION_PATH,
  LAST_PUBLISHED_AT,
  DEFAULT_LIMIT,
  PAGINATION,
};
