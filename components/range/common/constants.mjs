const BASE_URL = "https://api.range.co";
const VERSION_PATH = "/v1";
const MAX_RESOURCES = 1000;
const LAST_AFTER_TIME = "lastAfterTime";
const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  MAX_RESOURCES,
  LAST_AFTER_TIME,
  DEFAULT_LIMIT,
};
