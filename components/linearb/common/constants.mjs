const BASE_URL = "https://public-api.linearb.io";
const VERSION_PATH = {
  V1: "/api/v1",
  V2: "/api/v2",
};
const DEFAULT_LIMIT = 50;
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_MAX = 600;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
  LAST_CREATED_AT,
};
