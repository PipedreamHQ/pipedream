const BASE_URL = "https://yoplanning.pro";
const VERSION_PATH = "/api/v3.1";
const DEFAULT_LIMIT = 100;
const DEFAULT_MAX = 600;
const LAST_CREATION_DATE = "lastCreationDate";
const LAST_UPDATE = "lastUpdate";

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
  LAST_CREATION_DATE,
  LAST_UPDATE,
};
