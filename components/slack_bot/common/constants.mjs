import constants from "../../slack/common/constants.mjs";

const LAST_TIMESTAMP = "lastTimestamp";

const RESOURCE_NAME = {
  MESSAGES: "messages",
};

export default {
  ...constants,
  LAST_TIMESTAMP,
  RESOURCE_NAME,
};
