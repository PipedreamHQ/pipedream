const BASE_URL = "https://api.tower.nf";
const DEFAULT_LIMIT = 50;
const DEFAULT_MAX = 600;
const SOURCES = [
  "github",
  "tower",
];

export default {
  BASE_URL,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
  SOURCES,
};
