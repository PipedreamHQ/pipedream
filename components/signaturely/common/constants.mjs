const SUMMARY_LABEL = "$summary";
const BASE_URL = "https://api.signaturely.com";
const VERSION_PATH = "/api/v1";
const WEBHOOK_ID = "webhookId";

const SEARCH_TYPE = {
  DOCS: "documents",
  SIGNERS: "signers",
};

const DOC_STATUS = {
  DRAFT: "draft",
  AWAITING: "awaiting",
  COMPLETED: "completed",
};

export default {
  SUMMARY_LABEL,
  BASE_URL,
  VERSION_PATH,
  WEBHOOK_ID,
  SEARCH_TYPE,
  DOC_STATUS,
};
