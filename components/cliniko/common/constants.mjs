const SHARD_PLACEHOLDER = "{shard}";
const BASE_URL = `https://api.${SHARD_PLACEHOLDER}.cliniko.com`;
const VERSION_PATH = "/v1";
const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  SHARD_PLACEHOLDER,
  DEFAULT_LIMIT,
};
