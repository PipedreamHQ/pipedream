const BASE_URL = "https://public-api.shiphero.com";
const VERSION_PATH = "/graphql";
const DEFAULT_LIMIT = 20;
const SHARED_SIGNATURE_SECRET = "sharedSignatureSecret";

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_LIMIT,
  SHARED_SIGNATURE_SECRET,
};
