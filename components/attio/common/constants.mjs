const BASE_URL = "https://api.attio.com";
const VERSION_PATH = "/v2";
const DEFAULT_LIMIT = 100;

const TARGET_OBJECT = {
  PEOPLE: "people",
  COMPANIES: "companies",
  USERS: "users",
  DEALS: "deals",
  WORKSPACES: "workspaces",
};

const SEP = "_";

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_LIMIT,
  TARGET_OBJECT,
  SEP,
};
