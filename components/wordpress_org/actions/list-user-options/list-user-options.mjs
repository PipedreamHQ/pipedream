import wordpress_org from "../../wordpress_org.app.mjs";

export default {
  key: "wordpress_org-list-user-options",
  name: "List User Options",
  description: "Retrieves available options for the User field.",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    wordpress_org,
    page: {
      type: "integer",
      label: "Page",
      description: "The page of results to retrieve.",
      min: 0,
      default: 0,
    },
  },
  async run({ $ }) {
    const options = await wordpress_org.propDefinitions.user.options.call(this.wordpress_org, {
      page: this.page,
    });
    $.export("$summary", `Successfully retrieved ${options.length} option${options.length === 1
      ? ""
      : "s"}`);
    return options;
  },
};
