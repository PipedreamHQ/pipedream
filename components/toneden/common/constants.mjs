const BASE_URL = "https://www.toneden.io";
const VERSION_PATH = "/api/v1";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_LIMIT = 50;
const DEFAULT_MAX = 600;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
  LAST_CREATED_AT,
};
