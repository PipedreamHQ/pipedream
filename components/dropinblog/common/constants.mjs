const BLOG_PLACEHOLDER = "<blogId>";
const BASE_URL = "https://api.dropinblog.com";
const VERSION_PATH = `/v2/blog/${BLOG_PLACEHOLDER}`;
const LAST_DATE_AT = "lastCreatedAt";
const DEFAULT_MAX = 100;
const DEFAULT_LIMIT = 25;

export default {
  BLOG_PLACEHOLDER,
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
  LAST_DATE_AT,
};
