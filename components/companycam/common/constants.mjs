const BASE_URL = "https://api.companycam.com";
const VERSION_PATH = "/v2";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_MAX = 600;
const DEFAULT_LIMIT = 60;
const WEBHOOK_ID = "webhookId";
const TOKEN = "token";

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
  LAST_CREATED_AT,
  WEBHOOK_ID,
  TOKEN,
};
