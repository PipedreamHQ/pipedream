const ENVIRONMENT = {
  SANDBOX: "sandbox",
  PRODUCTION: "production",
};

const SANDBOX_PATH = "/sandbox";

const API_PATH = {
  DEFAULT: "/exp/v1",
  PAYMENTS: "/ipp/payments-gateway/v2",
  FRAUD: "/ipp/fraud/v1",
};

export default {
  ENVIRONMENT,
  SANDBOX_PATH,
  API_PATH,
};
