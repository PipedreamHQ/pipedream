import rinkel from "../../rinkel.app.mjs";

export default {
  key: "rinkel-list-recording-id-options",
  name: "List Recording ID Options",
  description: "Retrieves available options for the Recording ID field.",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    rinkel,
    page: {
      type: "integer",
      label: "Page",
      description: "The page of results to retrieve.",
      min: 0,
      default: 0,
    },
  },
  async run({ $ }) {
    const options = await rinkel.propDefinitions.recordingId.options.call(this.rinkel, {
      page: this.page,
    });
    $.export("$summary", `Successfully retrieved ${options.length} option${options.length === 1
      ? ""
      : "s"}`);
    return options;
  },
};
