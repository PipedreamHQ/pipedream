const BASE_URL = "https://api.getclarify.ai";
const VERSION_PATH = "/v1";
const WEBHOOK_ID = "webhookId";

const OBJECT_ENTITY = {
  PERSON: "person",
  COMPANY: "company",
  DEAL: "deal",
  MEETING: "meeting",
};

const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  OBJECT_ENTITY,
  WEBHOOK_ID,
  DEFAULT_LIMIT,
};
