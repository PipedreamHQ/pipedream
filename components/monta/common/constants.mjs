const ORDER_UPDATED_SINCE_STATUSES = [
  "NonDeleted",
  "Deleted",
];

const MIN_PAGE_SIZE = 1;
const MAX_PAGE_SIZE = 1000;
const DEFAULT_PAGE_SIZE = 30;

const LABEL_FILE_TYPES = [
  "pdf",
  "zpl",
];

export default {
  ORDER_UPDATED_SINCE_STATUSES,
  MIN_PAGE_SIZE,
  MAX_PAGE_SIZE,
  DEFAULT_PAGE_SIZE,
  LABEL_FILE_TYPES,
};
