import spiritme from "../../spiritme.app.mjs";

export default {
  key: "spiritme-list-voice-options",
  name: "List Voice Options",
  description: "Retrieves available options for the Voice field.",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    spiritme,
    page: {
      type: "integer",
      label: "Page",
      description: "The page of results to retrieve.",
      min: 0,
      default: 0,
    },
  },
  async run({ $ }) {
    const options = await spiritme.propDefinitions.voice.options.call(this.spiritme, {
      page: this.page,
    });
    $.export("$summary", `Successfully retrieved ${options.length} option${options.length === 1
      ? ""
      : "s"}`);
    return options;
  },
};
