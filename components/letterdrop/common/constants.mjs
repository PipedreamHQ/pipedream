const BASE_URL = "https://api.letterdrop.com";
const VERSION_PATH = "/api/v1";
const DEFAULT_MAX = 600;
const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
};
