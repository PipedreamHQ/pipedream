const BASE_URL = "https://api.46elks.com";
const VERSION_PATH = "/a1";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_MAX = 600;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  LAST_CREATED_AT,
};
