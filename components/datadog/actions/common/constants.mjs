const MetricType = {
  COUNT: "count",
  GAUGE: "gauge",
  RATE: "rate",
};

const metricTypes = [
  MetricType.COUNT,
  MetricType.GAUGE,
  MetricType.RATE,
];

export default {
  MetricType,
  metricTypes,
};
