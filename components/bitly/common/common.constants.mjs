const ALLOWED_DEEPLINK_KEYS = [
  "app_id",
  "app_uri_path",
  "install_url",
  "install_type",
];

const BASE_URL = "https://api-ssl.bitly.com/v4";

export default {
  ALLOWED_DEEPLINK_KEYS,
  BASE_URL,
};
