import checkvist from "../../checkvist.app.mjs";

export default {
  key: "checkvist-list-list-id-options",
  name: "List List ID Options",
  description: "Retrieves available options for the List ID field.",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    checkvist,
  },
  async run({ $ }) {
    const options = await checkvist.propDefinitions.listId.options.call(this.checkvist, {});
    $.export("$summary", `Successfully retrieved ${options.length} option${
      options.length === 1
        ? ""
        : "s"
    }`);
    return options;
  },
};
