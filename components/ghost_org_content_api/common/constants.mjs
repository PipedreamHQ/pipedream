const VERSION_PATH = "/ghost/api/content";
const DEFAULT_LIMIT = 100;
const MAX_RESOURCES = 200;
const LAST_CREATED_AT = "lastCreatedAt";

export default {
  VERSION_PATH,
  LAST_CREATED_AT,
  MAX_RESOURCES,
  DEFAULT_LIMIT,
};
