const BASE_URL = "https://api.heygen.com";
const VERSION_1_PATH = "/v1";
const VERSION_2_PATH = "/v2";

const TALKING_PHOTO_STYLES = [
  "square",
  "circle",
];

const TALKING_STYLES = [
  "stable",
  "expressive",
];

const EXPRESSIONS = [
  "default",
  "happy",
];

const DELAY = 1000 * 10;

export default {
  BASE_URL,
  VERSION_1_PATH,
  VERSION_2_PATH,
  TALKING_PHOTO_STYLES,
  TALKING_STYLES,
  EXPRESSIONS,
  DELAY,
};
