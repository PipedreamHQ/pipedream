const BASE_URL = "https://rest.smoove.io";
const VERSION_PATH = "/v1";

const MAX_RESOURCES = 600;
const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  MAX_RESOURCES,
  DEFAULT_LIMIT,
};
