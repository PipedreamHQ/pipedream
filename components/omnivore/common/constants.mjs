const BASE_URL = "https://api-prod.omnivore.app";
const VERSION_PATH = "/api/graphql";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_MAX = 600;
const DEFAULT_LIMIT = 20;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  LAST_CREATED_AT,
  DEFAULT_LIMIT,
};
