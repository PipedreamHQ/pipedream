const BASE_URL = "https://api.leadfeeder.com";

const DEFAULT_LIMIT = 100;

const PARAM = {
  PAGE_NUMBER: "page[number]",
  PAGE_SIZE: "page[size]",
};

export default {
  BASE_URL,
  PARAM,
  DEFAULT_LIMIT,
};
