import app from "../../polar.app.mjs";

export default {
  key: "polar-create-checkout",
  name: "Create Checkout",
  description: "Create a checkout session for one or more products. [See the documentation](https://polar.sh/docs/api-reference/checkouts/create-session)",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: false,
  },
  props: {
    app,
    products: {
      type: "string[]",
      label: "Products",
      description: "List of product IDs available to select at checkout. Example: `[\"prod_123\", \"prod_456\"]`. The first product is selected by default.",
    },
    prices: {
      type: "object",
      label: "Prices",
      description: "Optional mapping of product IDs to ad-hoc prices to create for those products. Example: `{ \"prod_123\": [{ \"amountType\": \"fixed\", \"priceAmount\": 1000, \"priceCurrency\": \"usd\" }] }`.",
      optional: true,
    },
    amount: {
      type: "integer",
      label: "Amount",
      description: "Custom checkout amount in cents for products using custom pricing. Example: `1000` for $10.00.",
      optional: true,
    },
    seats: {
      type: "integer",
      label: "Seats",
      description: "Number of seats for seat-based pricing. Required for seat-based products.",
      optional: true,
    },
    trialInterval: {
      type: "string",
      label: "Trial Interval",
      description: "Interval unit for the trial period.",
      options: [
        "day",
        "week",
        "month",
        "year",
      ],
      optional: true,
    },
    trialIntervalCount: {
      type: "integer",
      label: "Trial Interval Count",
      description: "Number of interval units for the trial period. Example: `14` with a trial interval of `day`.",
      optional: true,
    },
    allowTrial: {
      type: "boolean",
      label: "Allow Trial",
      description: "Whether to enable the trial period for the checkout session. If false, the trial is disabled even if the selected product has one configured.",
      optional: true,
    },
    discountId: {
      type: "string",
      label: "Discount ID",
      description: "ID of the discount to apply to the checkout.",
      optional: true,
    },
    customerId: {
      propDefinition: [
        app,
        "customerId",
      ],
    },
    customerEmail: {
      type: "string",
      label: "Customer Email",
      description: "Pre-fill the customer's email address in the checkout form.",
      optional: true,
    },
    customerName: {
      type: "string",
      label: "Customer Name",
      description: "Pre-fill the customer's name in the checkout form.",
      optional: true,
    },
    customerIpAddress: {
      type: "string",
      label: "Customer IP Address",
      description: "Pre-fill the customer's IP address for the checkout session.",
      optional: true,
    },
    customerBillingName: {
      type: "string",
      label: "Customer Billing Name",
      description: "Pre-fill the billing name for the checkout session.",
      optional: true,
    },
    customerBillingAddress: {
      type: "object",
      label: "Customer Billing Address",
      description: "Pre-fill the customer's billing address. Example: `{ \"line1\": \"123 Main St\", \"city\": \"New York\", \"state\": \"NY\", \"postalCode\": \"10001\", \"country\": \"US\" }`.",
      optional: true,
    },
    customerTaxId: {
      type: "string",
      label: "Customer Tax ID",
      description: "Pre-fill the customer's tax ID for the checkout session.",
      optional: true,
    },
    customerMetadata: {
      type: "object",
      label: "Customer Metadata",
      description: "Additional key-value data copied to the created customer. Example: `{ \"crmId\": \"cust_123\" }`.",
      optional: true,
    },
    isBusinessCustomer: {
      type: "boolean",
      label: "Is Business Customer",
      description: "Whether the customer is a business. If true, the customer must provide their full billing address and billing name.",
      optional: true,
    },
    externalCustomerId: {
      type: "string",
      label: "External Customer ID",
      description: "Customer ID from your system. Example: `crm_cust_98765`. If a matching Polar customer exists, the order is linked to it.",
      optional: true,
    },
    successUrl: {
      type: "string",
      label: "Success URL",
      description: "URL where the customer will be redirected after a successful payment. You can include `checkout_id={CHECKOUT_ID}`.",
      optional: true,
    },
    returnUrl: {
      type: "string",
      label: "Return URL",
      description: "URL used by the checkout back button.",
      optional: true,
    },
    embedOrigin: {
      type: "string",
      label: "Embed Origin",
      description: "Origin of the page embedding the checkout session, used to allow Polar's iframe to communicate with the parent page.",
      optional: true,
    },
    locale: {
      type: "string",
      label: "Locale",
      description: "Locale to use for the checkout session. Example: `en-US`.",
      optional: true,
    },
    currency: {
      type: "string",
      label: "Currency",
      description: "Presentment currency for the checkout session.",
      options: [
        "aud",
        "brl",
        "cad",
        "chf",
        "eur",
        "inr",
        "gbp",
        "jpy",
        "sek",
        "usd",
      ],
      optional: true,
    },
    subscriptionId: {
      type: "string",
      label: "Subscription ID",
      description: "ID of a subscription to upgrade. It must be on free pricing.",
      optional: true,
    },
    allowDiscountCodes: {
      type: "boolean",
      label: "Allow Discount Codes",
      description: "Whether customers can apply discount codes during checkout.",
      optional: true,
    },
    requireBillingAddress: {
      type: "boolean",
      label: "Require Billing Address",
      description: "Whether customers must fill their full billing address.",
      optional: true,
    },
    metadata: {
      type: "object",
      label: "Metadata",
      description: "Additional key-value data copied to the resulting order or subscription. Example: `{ \"orderId\": \"12345\", \"campaignId\": \"summer-sale\" }`.",
      optional: true,
    },
    customFieldData: {
      type: "object",
      label: "Custom Field Data",
      description: "Key-value object storing custom field values. Example: `{ \"company_size\": \"50-100\" }`.",
      optional: true,
    },
  },
  async run({ $ }) {
    const checkout = await this.app.createCheckout({
      $,
      products: this.products,
      prices: this.prices,
      amount: this.amount,
      seats: this.seats,
      trial_interval: this.trialInterval,
      trial_interval_count: this.trialIntervalCount,
      allow_trial: this.allowTrial,
      discount_id: this.discountId,
      customer_id: this.customerId,
      customer_email: this.customerEmail,
      customer_name: this.customerName,
      customer_ip_address: this.customerIpAddress,
      customer_billing_name: this.customerBillingName,
      customer_billing_address: this.customerBillingAddress,
      customer_tax_id: this.customerTaxId,
      customer_metadata: this.customerMetadata,
      is_business_customer: this.isBusinessCustomer,
      external_customer_id: this.externalCustomerId,
      success_url: this.successUrl,
      return_url: this.returnUrl,
      embed_origin: this.embedOrigin,
      locale: this.locale,
      currency: this.currency,
      subscription_id: this.subscriptionId,
      allow_discount_codes: this.allowDiscountCodes,
      require_billing_address: this.requireBillingAddress,
      metadata: this.metadata,
      custom_field_data: this.customFieldData,
    });

    $.export("$summary", `Successfully created checkout${checkout?.id
      ? ` with ID ${checkout.id}`
      : ""}`);
    return checkout;
  },
};
