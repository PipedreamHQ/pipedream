const BASE_URL = "https://api.algodocs.com";
const VERSION_PATH = "/v1";
const LAST_DATE_AT = "lastDateAt";
const IS_FIRST_RUN = "isFirstRun";
const DEFAULT_LIMIT = 20;
const DEFAULT_MAX = 10000;

export default {
  BASE_URL,
  VERSION_PATH,
  LAST_DATE_AT,
  IS_FIRST_RUN,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
};
