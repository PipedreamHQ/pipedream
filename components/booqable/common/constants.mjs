const SUMMARY_LABEL = "$summary";
const SUBDOMAIN_PLACEHOLDER = "{subdomain}";
const BASE_URL = `https://${SUBDOMAIN_PLACEHOLDER}.booqable.com`;
const VERSION_PATH = "/api/1";
const DEFAULT_MAX = 600;
const DEFAULT_LIMIT = 60;

export default {
  SUMMARY_LABEL,
  SUBDOMAIN_PLACEHOLDER,
  BASE_URL,
  VERSION_PATH,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
};
