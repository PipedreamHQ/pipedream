import spotify from "../../spotify.app.mjs";

export default {
  key: "spotify-list-saved-user-tracks-id-options",
  name: "List Track ID Options",
  description: "Retrieves available options for the Track ID field.",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    spotify,
    page: {
      type: "integer",
      label: "Page",
      description: "The page of results to retrieve.",
      min: 0,
      default: 0,
    },
  },
  async run({ $ }) {
    const options = await spotify.propDefinitions.savedUserTracksId.options.call(this.spotify, {
      page: this.page,
    });
    $.export("$summary", `Successfully retrieved ${options.length} option${options.length === 1
      ? ""
      : "s"}`);
    return options;
  },
};
