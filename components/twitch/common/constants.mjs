export default {
  // Page size for the search/channels async-options dropdown.
  SEARCH_CHANNELS_PAGE_SIZE: 20,
  // Page size for the users/blocks async-options dropdown (Twitch caps `first` at 100).
  USER_BLOCKS_PAGE_SIZE: 100,
};
