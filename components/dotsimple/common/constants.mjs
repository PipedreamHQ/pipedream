const BASE_URL = "https://app.dotsimple.io";
const WORKSPACE_PLACEHOLDER = "<workspaceId>";
const VERSION_PATH = `/app/api/${WORKSPACE_PLACEHOLDER}`;
const DEFAULT_MAX = 100;
const DEFAULT_LIMIT = 20;

export default {
  BASE_URL,
  VERSION_PATH,
  WORKSPACE_PLACEHOLDER,
  DEFAULT_MAX,
  DEFAULT_LIMIT,
};
