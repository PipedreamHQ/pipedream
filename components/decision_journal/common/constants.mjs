const BASE_URL = "https://api.decisionjournalapp.com";
const VERSION_PATH = "/v1";
const LAST_CREATED_AT = "lastCreatedAt";
const DEFAULT_LIMIT = 100;

export default {
  BASE_URL,
  VERSION_PATH,
  DEFAULT_LIMIT,
  LAST_CREATED_AT,
};
