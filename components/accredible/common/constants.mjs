const BASE_URL = "https://api.accredible.com";
const API = {
  V1: "/v1",
  V2: "/v2",
};
const LAST_ISSUED_ON = "lastIssuedOn";
const LAST_UPDATED_AT = "lastUpdatedAt";
const DEFAULT_LIMIT = 50;
const DEFAULT_MAX = 600;

export default {
  BASE_URL,
  API,
  DEFAULT_LIMIT,
  DEFAULT_MAX,
  LAST_ISSUED_ON,
  LAST_UPDATED_AT,
};
