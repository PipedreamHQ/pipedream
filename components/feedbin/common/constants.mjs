const BASE_URL = "https://api.feedbin.com";
const VERSION_PATH = "/v2";

const DEFAULT_LIMIT = 100;
const MAX_RESOURCES = 200;
const LAST_CREATED_AT = "lastCreatedAt";

export default {
  BASE_URL,
  VERSION_PATH,
  LAST_CREATED_AT,
  MAX_RESOURCES,
  DEFAULT_LIMIT,
};
