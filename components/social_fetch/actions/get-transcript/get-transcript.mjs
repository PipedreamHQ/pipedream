// @generated by integrations/pipedream/scripts/generate-from-catalog.mts — do not edit

import app from "../../social_fetch.app.mjs";

export default {
  key: "social_fetch-get-transcript",
  name: "Get Transcript",
  description: "Fetches a transcript for a video or post URL. [See the documentation](https://www.socialfetch.dev/docs/api)",
  version: "0.0.1",
  type: "action",
  annotations: {
    destructiveHint: false,
    openWorldHint: true,
    readOnlyHint: true,
  },
  props: {
    app,
    platform: {
      propDefinition: [
        app,
        "transcriptPlatform",
      ],
    },
    mediaUrl: {
      propDefinition: [
        app,
        "mediaUrl",
      ],
    },
  },
  async run({ $ }) {
    const response = await this.app.getTranscript({
      $,
      platform: this.platform,
      mediaUrl: this.mediaUrl,
    });
    $.export("$summary", "Successfully fetched transcript");
    return response;
  },
};
