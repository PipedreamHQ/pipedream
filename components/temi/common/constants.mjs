const BASE_URL = "https://api.temi.com";
const VERSION_PATH = "/v1";
const CREATED_ON = "createdOn";

const DEFAULT_PAGE_LIMIT = 100;
const DEFAULT_MAX_ITEMS = DEFAULT_PAGE_LIMIT * 3;

export default {
  BASE_URL,
  VERSION_PATH,
  CREATED_ON,
  DEFAULT_PAGE_LIMIT,
  DEFAULT_MAX_ITEMS,
};
