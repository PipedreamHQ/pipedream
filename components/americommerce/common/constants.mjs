const SUBDOMAIN_PLACEHOLDER = "{subdomain}";
const BASE_URL = `https://${SUBDOMAIN_PLACEHOLDER}.americommerce.com`;
const VERSION_PATH = "/api/v1";
const WEBHOOK_ID = "webhookId";

export default {
  SUBDOMAIN_PLACEHOLDER,
  BASE_URL,
  VERSION_PATH,
  WEBHOOK_ID,
};
